apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
spec:
  type: NodePort
  selector:
    app: portfolio
  ports:
    - port: 80
      targetPort: 3000       # This must match the containerPort from the deployment.yaml
      nodePort: 30080      # The port you will type in your browser
